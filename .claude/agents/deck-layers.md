---
name: deck-layers
description: >
  Implements all deck.gl data visualisation layers for NeMo-Ray: coverage
  heatmap, dead-zone GeoJSON, mast scatterplot, proposal columns. Uses typed
  data from types/coverage.ts (which Agent D will create; use the shape below
  as a local stub).
isolation: worktree
tools:
  - Read
  - Write
  - Edit
  - Bash
---
